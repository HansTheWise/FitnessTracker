# Core Framework
fastapi==0.115.0
uvicorn[standard]==0.32.0  # Enthält uvloop & httptools für Performance

# Database - ASYNC ist wichtig!
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0  # Async PostgreSQL Driver (WICHTIG: Ersetzt psycopg für async)
# psycopg[binary] NICHT nutzen bei async SQLAlchemy!

# Authentication & Security
pyjwt==2.9.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# Validation & Settings
pydantic==2.10.0
pydantic-settings==2.6.0

# Utilities
python-dotenv==1.0.1
python-multipart==0.0.18  # Für File Uploads & Form Data

# Health Checks & Monitoring
httpx==0.27.2  # Async HTTP Client (besser als requests für FastAPI)

# Development (optional, auskommentieren für Production)
# pytest==8.3.3
# pytest-asyncio==0.24.0
# httpx==0.27.2  # Für API Tests